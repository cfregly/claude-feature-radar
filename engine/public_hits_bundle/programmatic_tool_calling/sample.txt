
  Programmatic tool calling: the same customer-evidence task, Claude with programmatic tool calling vs without it.

  Task: from this week's support tickets, product logs, usage metering, CRM notes, and compliance docs,
  find the three customer accounts most likely to churn or block expansion.
  Same task, same model (Sonnet 4.6). Every number is read live off the API usage object.

  mode             billed input tokens    what happens to the 600 raw evidence rows
  ---------------------------------------------------------------------------------
  without programmatic tool calling                  54,989    all rows flow through the model context
  with programmatic tool calling                     14,299    sandbox reduces, only the packet returns
  ---------------------------------------------------------------------------------

  -> 74% fewer billed input tokens, because the raw evidence rows went to the
     sandbox, not the model context. The saving compounds across every fan-out.

  Expected compact decision:
    acct_1842, acct_2199, acct_7731

  Estimated token/API run cost: $0.08.
  Token/API dollars include weighted cache buckets when present: uncached input,
  cache reads, cache writes, output, and server-tool charges from the usage object.
  Code execution runtime can bill separately after the monthly free allowance.
  Outside that allowance, one new billed programmatic tool calling container has a 5-minute floor exposure
  of $0.0042 at $0.05 per hour. Exact invoice impact depends on free
  allowance, container reuse, runtime, and whether the request is paired with web
  search or web fetch.

  Trace summary:
    - trace fields are emitted by the demo runner, not slide-only claims.
    - Mode A: caller_path=direct raw_tool_bytes=148,305 final_bytes=5,226 containers=0 server_tool_blocks=0 code_execution_requests=0 caller_path_drift=False correctness=True
    - Mode B: caller_path=code_execution_20260120 raw_tool_bytes=148,305 final_bytes=3,895 containers=1 server_tool_blocks=2 code_execution_requests=0 caller_path_drift=False correctness=True
    - Trace gate: PASS caller path, server block, container, and fallback checks

  The change is two lines: add the code_execution tool, then put
  allowed_callers: ["code_execution_20260120"] on your own tool.

  Runnable code and the full artifact: programmatic_tool_calling/README.md
