  Claude task budgets: stop before a budget-exhausted tool loop.

  Workload: start a 12-record audit by calling fetch_record(1), unless the hidden
  task budget says the loop is near exhausted. Model: claude-opus-4-8.
  The change: output_config={"task_budget": {"type": "tokens", "total": 20000}}

  arm                       hidden low-budget stop   first tool calls   wall time
  -------------------------------------------------------------------------------
  low budget (remaining 50)            yes                  0              2.1s
  control   (remaining 5000)           n/a                  1              1.6s

  Claude saw the low remaining budget and handed off before the first tool call.
  With budget to spare, the same agent started the loop.

  The agent stops at the right moment on its own, so a long job ends with a clean
  handoff instead of a tool call it cannot pay for.
  cost $0.01 on Claude Opus 4.8 for the two-call check.

  Runnable code and the full brief: task_budgets/README.md
