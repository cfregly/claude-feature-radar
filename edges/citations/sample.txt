
  Verifiable source citations: 3 of your own documents, 8 questions.
  Claude Citations is GA, no beta header. This run measures Claude's structured source pointers
  on plain-text documents: the grader verifies the API's own offsets against the source
  (source[start:end] == cited_text).

  what a founder building over their own documents gets
  arm                                  resolves   resolver  quote free  out_tok     cost$
  ----------------------------------------------------------------------------------------
  Claude Haiku 4.5 + Citations              8/8    the API         yes      305  $0.01

  Honest reading:
  - Claude Haiku 4.5 + Citations: 8/8 pointers resolve, the API does the resolving and guarantees it,
    and the quote is free of output tokens. Zero resolver code.
  - The value is the in-API guarantee, the free quote, and char granularity on plain-text documents,
    a within-Claude value-add.
  - The cross-vendor head-to-heads live in the sibling citations edges, where each task shape states
    whether it uses direct request inputs, hosted search, PDFs, returned RAG chunks, or mixed grounding.

  (per-turn detail cached in gitignored data/last_citations.json; this printout is the receipt)
