
  Parity-gated candidates (the long tail). Each is HELD until its parity check survives.
  Run the skeptic pass (engine/verify.py), then record the verdict here. $0 view, no API call.

  === fallback_credit [correctness] ===
    parity check: unchecked  ->  HELD (never-evaluated, not pitched)
    Claude surface:     server-side model fallback (Fable/Mythos) with a fallback credit: recover from a refusal by falling back to another model inside a single call
    parity-check vs:    client-side retry loop a founder builds on a competitor (no named single-call server-side cross-model fallback with a credit)
    thin proof (gated): trigger a refusal/failure on the primary model, show the server-side fallback recovering in ONE call with the credit applied, vs the client-side retry loop
    score gate:         recovery_happened_in_one_call AND credit_applied
    note:               Fable/Mythos may be access-gated (a dev key reaches Haiku/Sonnet/Opus, not Fable without Mythos access); an inaccessible tier is reported unavailable, never faked
    source:             https://platform.claude.com/docs/en/api/openai-sdk (2026-06-18)

  === cache_diagnostics [observability] ===
    parity check: unchecked  ->  HELD (never-evaluated, not pitched)
    Claude surface:     per-request cache_miss_reason in the usage object: an explainer of WHY a cache read missed (prefix below the minimum, changed prefix, expired TTL)
    parity-check vs:    OpenAI and Gemini usage objects (no named per-request cache-miss-reason field surfaced to the caller)
    thin proof (gated): construct each cache-miss condition deliberately and show Claude reports the miss reason per request where the competitor usage object is silent, turning a silent cost leak into a debuggable signal
    score gate:         miss_reason_present_on_claude AND absent_on_every_fetched_competitor
    note:               a narrow observability candidate; confirm the field name against the live usage schema before quoting, it can move
    source:             https://platform.claude.com/docs/en/build-with-claude/prompt-caching (2026-06-18)

  === build_velocity [agentic-success] ===
    parity check: unchecked  ->  HELD (never-evaluated, not pitched)
    Claude surface:     Claude Code as a programmable build surface: the @claude GitHub Action opens PRs following CLAUDE.md, runs review on every PR, runs headless in CI; plugins bundle skills, agents, hooks, and MCP as one marketplace unit
    parity-check vs:    OpenAI Codex CLI and SKILL.md, Google Gemini CLI (every headline primitive now has a shipping competitor equivalent)
    thin proof (gated): time the end-to-end loop, issue to merged PR following CLAUDE.md headless in CI, against Codex CLI and Gemini CLI on the same repo and issue, counting human-intervention steps and glue-code lines
    score gate:         pr_merged_following_claude_md AND fewer_human_steps_than_competitor
    note:               the master brief flags this is hard to prove with one clean number and every headline primitive now has a competitor equivalent, so it is treated as supporting color, not an anchored head-to-head; parity is the likely read
    source:             https://docs.claude.com/en/docs/claude-code/github-actions (2026-06-18)

  3 of 3 candidate(s) held: the parity check has not survived, so they
  stay never-evaluated and are never pitched. This is the honest state of the long tail.

