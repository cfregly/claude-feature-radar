
  Programmatic tool calling: the input-token receipt on a real fan-out task.
  Task: across 4 regions, each returning ~60 sales rows, find the
  region with the highest total revenue. True winner (computed locally): east.
  Same task and same model (Claude Sonnet 4.6) both ways, the final answer must match or we fail.

  running Mode A (plain tool use, every row through context) ...
      [A] turn 1: stop=tool_use tool_use=4 code=0 billed=745 3s
      [A] turn 2: stop=end_turn tool_use=0 code=0 billed=9,451 9s
    Mode A done: 2 round-trips, 9,451 billed input tokens, answer None.
  running Mode B (programmatic, rows filtered in the sandbox) ...
      [B] turn 1: stop=tool_use tool_use=1 code=1 billed=3,294 6s
      [B] turn 2: stop=tool_use tool_use=1 code=0 billed=3,294 8s
      [B] turn 3: stop=tool_use tool_use=1 code=0 billed=3,294 9s
      [B] turn 4: stop=tool_use tool_use=1 code=0 billed=3,294 10s
      [B] turn 5: stop=end_turn tool_use=0 code=0 billed=6,828 14s
    Mode B done: 5 round-trips, 6,828 billed input tokens, answer east.

  mode                                      billed input tok  round-trips    answer       cost
  --------------------------------------------------------------------------------------------
  Mode A: plain tool use                               9,451            2      None    $0.0602
  Mode B: programmatic (allowed_callers)               6,828            5      east    $0.0233

  Honest reading:
  - THE EDGE, measured: Mode B billed 6,828 input tokens vs Mode A's 9,451, a 28% reduction, because the 240 rows the tool returned went to the sandbox, not the model context.
  - Bonus, correctness: Mode B (the sandbox code does the arithmetic) answered east (correct). Mode A (the model sums 240 rows in its head) answered None (WRONG). Exact math in code beats in-context summation over many rows.
  - Honest cost of the approach: Mode B made 5 model round-trips to Mode A's 2 because the model called the tool serially from code, so on an INSTANT mock tool it runs slower. The token saving is the win, the latency depends on your real tool's per-call time (PTC saves the per-call model round-trip a real slow tool would cost).
  - Fan-out-shaped: the doc notes a sequential single-call task (tau2-bench) is flat to about 8% more expensive. No competitor keeps your custom-tool outputs out of context.

  wrote receipts to data/last_programmatic_tool_calling.json

